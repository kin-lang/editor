export interface EditorOptions{
    height: string
    width: string
    defaultLanguage: string
    theme: string,
    defaultValue: string   
}

export interface Program{
    name: string
    code: string
}